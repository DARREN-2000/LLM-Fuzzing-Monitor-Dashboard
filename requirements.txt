# =============================================================
#  LLM Fuzz Monitor — Python Dependencies
# =============================================================
#  Install:  pip install -r requirements.txt
#  Dev:      pip install -r requirements-dev.txt
# =============================================================

# --- Core -----------------------------------------------------------
psutil>=5.9,<6.0
numpy>=1.24,<2.0
requests>=2.31,<3.0
PyYAML>=6.0,<7.0
portalocker>=2.7,<3.0

# --- CLI / TUI ------------------------------------------------------
rich>=13.0,<14.0
click>=8.1,<9.0

# --- File watching ---------------------------------------------------
watchdog>=3.0,<5.0

# --- Optional LLM providers (install as needed) ---------------------
# openai>=1.0,<2.0
# anthropic>=0.18,<1.0
# docker>=7.0,<8.0

# --- Optional performance -------------------------------------------
# lz4>=4.3,<5.0
# ujson>=5.8,<6.0
# aiofiles>=23.0,<24.0
# scipy>=1.11,<2.0
